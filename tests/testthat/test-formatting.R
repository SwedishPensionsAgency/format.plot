library(format.plot)

context("Plot output format")

test_that("Plots generate properly to LaTeX", {
  test_plotdata <- diamonds
  # expect_equal_to_reference(format.tables(test_plotdata), "test_plotoutput.rds")
  expect_equal(1, 1)
})